<%= 
  react_component("home/Index", {
    products: @products,
    categories: @categories,
    category: @category,
    is_admin: user_signed_in? && current_user.admin?,
    cart: @cart
  }, {
    prerender: true
  }) 
%>
