<%= 
  react_component("home/Index", {
    products: @products,
    categories: @categories,
    category: @category,
    cart: @cart,
    is_admin: user_signed_in? && current_user.admin?
  }, {
    prerender: true
  }) 
%>
